{
    "tasks": [
        {
            "type": "shell",
            "label": "Générer les fichiers du projet avec make",
            "command": "make",
            "args": [
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}